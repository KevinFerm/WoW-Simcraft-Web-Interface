== WoW Simcraft Web Interface

This is a basic implementation of a Simcraft web interface. It runs on rails and node.js.
To run this you need to setup your own simcraft cli so that you'll have the latest version.
